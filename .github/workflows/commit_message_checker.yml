name: Commit Message Checker
on:
  pull_request:
    branches:
      - master
      - 'release/**'

jobs:
  check-commit-message:
    permissions:
      contents: read
    uses: IsyFact/isy-github-actions-templates/.github/workflows/commit_message_checker_template.yml@v1.0.0
